---
"@axah/react-svg-editor": patch
---

Improves editor UX and maintainability with a flatter toolbar layout, more stable color picker popovers, and reduced UI shifts. Also refactors shared internal types/props across artboard, drawables, and cropables, plus tighter release validation checks.

